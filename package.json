{
  "name": "rust-wasm-compute-compare",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "cd rust-compute && wasm-pack build --target web --out-dir ../frontend/src/wasm",
    "dev": "npm run build:wasm && cd frontend && npm run dev",
    "build": "npm run build:wasm && cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install"
  },
  "devDependencies": {}
}

